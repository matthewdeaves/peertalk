{
  "skills": ["build"],
  "query": "Run tests with coverage",
  "files": ["Makefile", "tests/"],
  "expected_behavior": [
    "Runs make clean && make test to compile and execute tests",
    "Generates coverage report using lcov",
    "Runs quality gates checker (function length, file size, coverage threshold)",
    "Reports pass/fail status for each quality gate",
    "Displays coverage percentage and test results"
  ]
}
